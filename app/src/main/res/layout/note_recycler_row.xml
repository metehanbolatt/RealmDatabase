<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/rowLinear"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="180dp"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="10dp"
    android:padding="10dp"
    android:background="@drawable/note_row_background"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/noteTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="#E65100"
            android:text="Ödevlerini Yap"
            android:fontFamily="@font/gotham_bold"
            android:gravity="start"
            android:layout_marginEnd="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/closeButton"/>

        <ImageView
            android:id="@+id/closeButton"
            android:layout_width="15dp"
            android:layout_height="15dp"
            android:src="@drawable/ic_close"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>


    <View
        android:layout_width="wrap_content"
        android:layout_height="1dp"
        android:background="#9F9F9F"
        android:layout_marginTop="5dp"/>

    <TextView
        android:id="@+id/noteDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="5"
        android:fontFamily="@font/gotham_book"
        android:textColor="@color/white"
        android:layout_marginTop="5dp"
        android:lineSpacingExtra="2dp"
        android:text="Matematik türkçe fen coğrafya tarih beden müzik ingilizce almanca sosyal bilgiler türk dili ve edebiyatı geometri" />

</LinearLayout>